// Placeholder — build.js overwrites the compiled copy of this file with the
// bundled source of worker.entry.ts. An empty string here means "not built
// yet"; workerManager falls back to the legacy runtime code generator in that
// case (e.g. when running straight from tsc output without js-build).
export const workerSource = ''
